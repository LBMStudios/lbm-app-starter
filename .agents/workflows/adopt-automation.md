# Adopt LBM automation

Use the shared `adopt-automation` skill on the current repository. Start with `pnpm stack:adopt -- --target <path>` from the starter, review its dry-run matrix before editing, preserve project-specific configuration, implement only approved compatible layers, run the repository's complete quality gate, and stop before deployment or production changes.
